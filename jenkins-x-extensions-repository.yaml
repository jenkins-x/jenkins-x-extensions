version: 6
extensions:
- name: hello-world
  version: 0.0.2
  description: Says Hello, World!
  post:
    always: |
      #!/bin/sh
      echo "Hello, World!"
- name: spotbugs-maven
  version: 0.0.4
  description: Run's SpotBugs against a Maven project and attaches the output to the build
  post:
    always: |
      #!/bin/sh
      set -x
      mvn compile com.github.spotbugs:spotbugs-maven-plugin:${SPOTBUGS_VERSION}:spotbugs
      set +x
